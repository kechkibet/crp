create table propertyregister (
    ID int generated by default as identity(start with 1) not null,
    CREATE_TS timestamp,
    CREATED_BY varchar(50),
    DELETE_TS timestamp,
    DELETED_BY varchar(50),
    UPDATE_TS timestamp,
    UPDATED_BY varchar(50),
    VERSION integer,
    --
    `lrNumber` varchar(255),
    `ratePayer` integer not null,
    `valuationRoll` integer not null,
    status integer,
    `physicalLocation` varchar(255),
    `gpsLongitude` varchar(255),
    `gpsLatitude` varchar(255),
    `gpsAltitude` varchar(255),
    `uploadStampDuty` varchar(36),
    `sellingValue` integer,
    `uploadTitleDeed` varchar(36),
    `sizeInHa` integer,
    `subCounty` integer,
    interest varchar(255),
    `leaseExpiry` integer,
    `leaseStartDate` date
);
